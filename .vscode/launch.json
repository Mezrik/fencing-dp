{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Wails: Debug fencing.app",
      "type": "go",
      "request": "launch",
      "mode": "exec",
      "program": "${workspaceFolder}/build/bin/fencing.app",
      "preLaunchTask": "build debug",
      "cwd": "${workspaceFolder}"
    }
  ]
}
