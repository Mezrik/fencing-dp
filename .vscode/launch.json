{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Wails: Debug fencing",
      "type": "go",
      "request": "launch",
      "mode": "exec",
      "program": "${workspaceFolder}/build/bin/fencing",
      "preLaunchTask": "build debug",
      "cwd": "${workspaceFolder}"
    }
  ]
}
